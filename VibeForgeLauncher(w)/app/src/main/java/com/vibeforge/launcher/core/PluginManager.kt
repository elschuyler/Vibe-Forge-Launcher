package com.vibeforge.launcher.core

import android.content.Context

class PluginManager(context: Context) {
    fun discoverAndBindPlugins() { }
    fun getActivePlugins(): List<Any> = emptyList()
}
